import LoginModal from './LoginModal';
import { StModalWrapper } from './LoginPagestyle';

const LoginPage = () => {
  return (
    <StModalWrapper>
      <LoginModal />
    </StModalWrapper>
  );
};

export default LoginPage;
